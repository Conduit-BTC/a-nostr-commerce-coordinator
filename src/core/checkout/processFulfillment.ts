// async function performFulfillment(order) {
// const shippingId = await createShipStationOrder(order);

// if (!shippingId) {
//     throw new Error(`Error updating order with shipment ID: ${order.id}`);
// }
// }
// Subscribe to events
// On event, decrypt and handle appropriately

export default async function subscribeToDirectMessagesLoader({
  container,
}: {
  container: NCCModuleContainer;
}) {
  console.log("Firing subscribeToDirectMessagesLoader...");
}
